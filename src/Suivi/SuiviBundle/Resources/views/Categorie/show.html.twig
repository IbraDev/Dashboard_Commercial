
{% extends '::base.html.twig' %}
{% block body %}
    {% include 'SuiviVenteBundle::header.html.twig' %}
    {% include 'SuiviVenteBundle::colonne-gauche.html.twig' %}
      <!-- Content Wrapper. Contains page content -->
      <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
          <h1>
          Gestion Catégories
            
          </h1>
          <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Accueil</a></li>
            <li class="active">Gestion Catégories</li>
            <li class="active">Lister</li>
          </ol>
        </section>

        <!-- Main content -->
        <section class="content">
          <div class="row">
            <!-- left column -->
            <div class="col-md-6">
              <!-- general form elements -->
              <div class="box box-primary">
              
                <div class="box-header">
                 
                  <h3 class="box-title">Catégorie</h3>
                </div><!-- /.box-header -->
                <!-- form start -->
              

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id :</th>
                <td>{{ entity.id }}</td>
            </tr>  
            <tr>
                <th>Nom : </th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Description :</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Datecreation :</th>
                <td>{{ entity.dateCreation|date('Y-m-d') }}</td>
            </tr>
        </tbody>
    </table></br>

        
    
        <a href="{{ path('categorie') }}">
           <img src="{{asset("bundles/adminTemplate/img/MB__back.png")}}">
        </a>
    
          <a href="{{ path('categorie_edit', { 'id': entity.id }) }}">
           <img src="{{asset("bundles/adminTemplate/img/edit.png")}}">
        </a>
        
    
    {{ form(delete_form) }}

              </div><!-- /.box -->
              
</section>
 </div>

    {% include 'SuiviVenteBundle::footer.html.twig' %}



{% endblock %}
