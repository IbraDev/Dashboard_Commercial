
{% extends '::base.html.twig' %}
{% block body %}
<title>Suivi Vente | Dashboard</title>


</head>
<body class="skin-blue">
<div class="wrapper">

    {% include 'SuiviVenteBundle::header.html.twig' %}
    {% include 'SuiviVenteBundle::colonne-gauche.html.twig' %}

    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <h1>
                Liste des admin

            </h1>
            <ol class="breadcrumb">
                <li><a href="#"><i class="fa fa-dashboard"></i> Accueil</a></li>
                <li class="active">Dashboard</li>
            </ol>
        </section>

        <!-- Main content -->
        {% block content %}
        {%endblock  %}
        <section class="content">

            
                <map name="carte1">
                    <area shape="rect" coords="10, 10, 20, 20" title="Consulter"></area>
                </map>
            <div class="module-body table">
                <table cellpadding="0" cellspacing="0" border="0" class="datatable-1 table table-bordered table-striped	 display"
                       width="100%">
                    <thead>
                <tr>

                    <th >Login</th>
                    <th >email</th>
                    <th >dernier login</th>
					<th>Role</th>
                    
                        <th>supprimer</th>
                    

                </tr>
				{% for user in users %}
                <tr class="active">
                <td >{{user.username}}</td>
                <td > {{user.email}}</td>
                <td >{{user.lastLogin|date('d/m/Y')}}</td>
				<td ></td>
               
                <td style="width: 50px">
                    <center><a href="" onClick=""  name="modal"><img src="{{asset('AdmAcceuil/images/suppr.jpg')}}" alt="picture" width="30" height="20" /></a></center>
                </td>
				 </tr>
				
				  {% endfor %}
                    </thead>
                    </table>
                
           
        </section>
    </div>


    {% include 'SuiviVenteBundle::footer.html.twig' %}



    {% endblock %}















